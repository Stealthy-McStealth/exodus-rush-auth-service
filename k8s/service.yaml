apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: passover
spec:
  type: ClusterIP
  ports:
  - port: 8083
    targetPort: 8083
    name: http
  selector:
    app: auth-service
